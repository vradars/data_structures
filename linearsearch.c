#include<stdio.h>
int main(void)
{
	int n,i,m;
	scanf("%d",n);
	int arr[n];
	int k;
	Scanf("%d",k)
		//ele to search
	for(i=0;i<n;i++)
	{
		Scanf("%d",arr[i]);
		if(arr[i]==k)
		m=i;
	}
	printf("%d",m);
	return 0;
}


